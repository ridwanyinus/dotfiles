{
  "background only": {
    "prefix": "background-only",
    "body": "\"background-only\"",
    "description": "a complete React runtime, handling component lifecycles and other side effects. Since complete consistency with single-threaded React is not possible, we have a modified component lifecycle"
  },
  "main thread": {
    "prefix": "main-thread",
    "body": "\"main thread\"",
    "description": "responsible for rendering the initial screen and applying subsequent UI updates. This allows users to see the first screen as quickly as possible while reducing the main thread's workload."
  },
  "bindtap": {
    "prefix": "bindtap",
    "body": "bindtap={$0}",
    "description": "bindtap event handler"
  },
  "catchtap": {
    "prefix": "catchtap",
    "body": "catchtap={$0}",
    "description": "catchtap event handler"
  },
  "auto scrolling": {
    "prefix": "auto-scrolling",
    "body": "$0.current?.invoke({\n\tmethod: \"autoScroll\",\n\tparams: {\n\t\tdirection: \"${1:up}\",\n\trate: \"${2:10|20|30|60|90}\"\n\tstart: true,\n},\n}).exec",
    "description": "bindtap event handler"
  }
}
