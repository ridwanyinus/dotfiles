#set -----------------------------------------------------
# CUSTOMIZATION
# -----------------------------------------------------

# -----------------------------------------------------
# Prompt
# -----------------------------------------------------
# eval "$($HOME/.local/bin/oh-my-posh init fish --config $HOME/.config/ohmyposh/zen.toml)"
eval "$($HOME/.local/bin/oh-my-posh init fish --config $HOME/.config/ohmyposh/custom_p10k_lean.omp.json)"
eval "$($HOME/.local/bin/oh-my-posh enable reload)"
